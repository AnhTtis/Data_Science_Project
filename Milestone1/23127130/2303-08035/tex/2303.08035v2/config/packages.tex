% REORDER IMPORTS, REMEMBER ABOUT PRECEDENCES

%\IEEEoverridecommandlockouts
% The preceding line is only needed to identify funding in the first footnote. If that is unneeded, please comment it out.

\usepackage[export]{adjustbox}
\usepackage{algorithm}
% a newer version for algorithmicx
\usepackage[%
    % whether to show vertical indentation guides
    indLines=false,
    % italic in comments
    italicComments=true,
    % whether to show end keyword for blocks
    noEnd=false,
]{algpseudocodex}
\usepackage{amsmath}
\usepackage[font=footnotesize,labelfont=bf]{caption}
% for circling text
\usepackage{circledsteps}
% use `` " for proper double-quotes, or \enquote{} from csquotes
\usepackage{csquotes}
% to insert custom dates with \newdate and \displaydate
% datetime is obsolete
\usepackage{datetime2}
% not needed when using stfloats
% \usepackage{dblfloatfix}
% these packages are required only to add a header/footer in the first page, which is useful for arXiV publications, such as "to appear at..."
\usepackage{fancyhdr}
% for right-side captions
% conflict with float
% \usepackage{floatrow}
% to fix footnotes
\usepackage{footmisc}
% to use acronyms
\usepackage[%
    %automake,  % to avoid errors with Overleaf if not using record for bib2gls
    toc, % puts the link in the ToC
    record, % to use bib2gls
    abbreviations, % to load abbreviations / acronyms
    nonumberlist, % to avoid printing the numbers of the references in the acronyms page
]{glossaries-extra}
\usepackage{graphicx}
% for hyperlinks, before biblatex if custom options are needed
\usepackage{hyperref}
% for relative imports, using \import{full-path}{relative-path} and \subimport{}{}
\usepackage{import}
\usepackage{makecell}
% to avoid using \emergencystretch, final to overwrite draft
% https://tex.stackexchange.com/q/171999
\usepackage[final]{microtype}
\usepackage{multirow}
\usepackage{newtxmath}  % it should substitute amssymb and amsfonts
% for better number printing
\usepackage{numprint}
% for check and x marks, ding 51 and 55, defined as greencheckmark and redxmark
\usepackage{pifont}
% for justifying text, use \justifying for fully-justified text
\usepackage{ragged2e}
% for side captions instead of bottom when using SCfigure
% compile timeout error on Overleaf
% \usepackage[rightcaption]{sidecap}
\usepackage{siunitx}
% improve positioning of figure* floats for top/bottom
\usepackage{stfloats}
\usepackage{svg}
\usepackage{tabularx}
\usepackage{textcomp}
\usepackage{tikz}
% dvipsnames is for extra colour names available at https://www.overleaf.com/learn/latex/Using_colours_in_LaTeX#Reference_guide
% other options are svgnames and x11names, each wider than the previous
% REF: https://mirror.easyname.at/ctan/macros/latex/contrib/xcolor/xcolor.pdf#section.4
% NOTE: we choose to use dvipsnames
% however it needs to be added in the document class
\usepackage{xcolor}

% this package is used to implement camelcase for abbreviations
% NOT WORKING
% \usepackage{mfirstuc}
