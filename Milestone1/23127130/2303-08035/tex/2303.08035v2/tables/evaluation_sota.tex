\begin{table}[h]
    \caption{Comparison between \emphasizedworkname{} and other \gls{sota} fault models: \emphasizedworkname{} does not require a-priori knowledge, and achieves slightly lower precision but in much fewer number of samples, while still being capable of reaching \num{100}\% recall.}
    \label{table:evaluation_sota}
    \centering
    % to change the vertical spacing between cells
    % https://tex.stackexchange.com/a/50337
    {\renewcommand{\arraystretch}{1.4}
    % use setlength to change the horizontal spacing between cells
    % it is local, i.e. it must be repeated in each environment
    \setlength{\tabcolsep}{2pt}
    \resizebox{\linewidth}{!}{%
        \import{tables/tabular/}{evaluation_sota}
    }
    }
\end{table}