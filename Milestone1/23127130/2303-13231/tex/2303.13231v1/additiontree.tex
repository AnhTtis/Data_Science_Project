\begin{tikzpicture}

\def\angle{45}

\node {$\sum_{\gind=1}^\ngrad \pgrad[\gind]$} [sibling distance = 5cm]
    child {node (a) {$\sum_{\gind=1}^{\ngrad/2}  \pgrad[\gind]$} [sibling distance = 2.5cm]
        child {node (b) {$\pgrad[1]+\pgrad[2]$}  [sibling distance = 1cm] edge from parent [draw=none]
            child {node {$\pgrad[1]$}}
            child {node (y) {$\pgrad[2]$}}
        }
        child {node (c) {\phantom{$\sum_{\gind=1}^{\ngrad/2}  \pgrad[\gind]$}}  [sibling distance = 1cm] edge from parent [draw=none]}
    } 
    child {node (d) {$\sum_{\gind=\ngrad/2+1}^{\ngrad}  \pgrad[\gind]$} [sibling distance = 2.5cm]
        child {node (e) {\phantom{$\sum_{\gind=1}^{\ngrad/2}  \pgrad[\gind]$}} [sibling distance = 1cm]  edge from parent [draw=none]}
        child {node (f) {$\pgrad[\ngrad-1] + \pgrad[\ngrad]$} [sibling distance = 1cm]  edge from parent [draw=none]
            child {node (z) {$\pgrad[\ngrad-1]$}}
            child {node {$\pgrad[\ngrad]$}}
        }
    };

    \path (a) -- (b) node [midway, rotate=-\angle] {$\vdots$};
    \path (a) -- (c) node [midway, rotate=\angle] {$\vdots$};
    
    \path (d) -- (e) node [midway, rotate=-\angle] {$\vdots$};
    \path (d) -- (f) node [midway, rotate=\angle] {$\vdots$};

    \path (y) -- (z) node [midway] {$\cdots$};    

\end{tikzpicture}
