\begin{figure*}[t]
\begin{center}
\includegraphics[width=0.94\linewidth]{latex/figures/model/model15.pdf}
\end{center}
\vspace{-9mm}
\caption{\textbf{Overall architecture of BlurHandNet.} BlurHandNet first unfolds input image~$\mathbf{I}$ into three temporal joint features~$\mathbf{F}_{{\text{J}}_{\text{E1}}}$, $\mathbf{F}_{{\text{J}}_{\text{E2}}}$, and $\mathbf{F}_{{\text{J}}_{\text{M}}}$.
The following kinematic temporal Transformer~(KTFormer) refines each joint feature by leveraging the attentive correlation between them.
% Finally, Regressor produces MANO~\cite{romero2022embodied} parameters.
% The temporal 3D hand meshes are obtained by forwarding the MANO parameters to MANO layer.
Finally, Regressor produces MANO~\cite{romero2022embodied} parameters for each time step, resulting in temporal 3D hand meshes.
}
\vspace{-3mm}
\label{fig:model}
\end{figure*}