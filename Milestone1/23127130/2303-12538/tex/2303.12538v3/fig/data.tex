\begin{figure}
    \centering
    \includegraphics[width=0.85\linewidth]{fig/data_fig.pdf}
    \vspace{-0.5em}    
    \caption{\textbf{Paired Data Generation:} Given an HOI  image, we first segment out hand (b) and remove it by inpainting (c). Then we use SDEdit~\cite{sdedit} to reduce inpainting artifact (d). As inpainting introduce discrepancy between mask and unmasked region (f) while SDEdit undesirably modifies the unmasked object region, we mix up \textit{both} object image sets in training. }
    \label{fig:data}
    \vspace{-1.5em}
\end{figure}