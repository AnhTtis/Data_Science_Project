\begin{figure*}[t]
    \centering
        \includegraphics[width=1.0 \linewidth]{figures/fig3.pdf} \\ \vspace{-10pt}
      \caption{Overall architecture of \method. The pretrained image transformer takes a grid-like frameset and $T_s$ frames as inputs. We learn the temporal and spatial contexts separately through two distinct paths.
      During training, we only update newly introduced adapters and the classifier while freezing the pretrained layers.}\vspace{-5pt}
    \label{fig:main}
\end{figure*}