%\vspace{-1mm}
\section{Conclusion}\label{sec:conclu}
\vspace{-1mm}

We have presented a novel, abstraction-based method, naturally coupled with a black-box verification algorithm, for efficiently training provably robust image classifiers.
% The training method
% can be used to train neural networks 
% takes input as intervals, which can make the trained neural networks more robust. 
The experimental results showed that our approach outperformed the state-of-the-art robust training approaches with up to 95.64\% improvement in reducing verified errors. 
Moreover, thanks to its black-box feature, our verification algorithm is more amenable and scalable to  
% black-box verification 
%approach can 
% verify 
%train and verify  
large neural networks with up to 138 million trainable parameters and is applicable to a wide range of neural networks.
% architecures.
% The experimental results have  also shown that our approach outperforms the state of the art in both effectiveness and efficiency. 
% A natural next step would be to utilize the information from the verification process to guide the refinement of the abstraction granularity, which could further improve the robustness of the trained network.

\iffalse 
We have presented a novel, abstraction-based training method, naturally coupled with a black-box verification algorithm, for training provably robust image classifiers.
% The training method
% takes input as intervals, which can make the trained neural networks more robust. 
Compared with the state-of-the-art robust training approaches, our approach can achieve up to 95.64\% improvement in reducing verified errors. 
Moreover, our 
approach can 
train and verify 
large neural networks with up to 138 million trainable parameters and is applicable to a wide range of neural networks.
% A natural next step would be to utilize the information from the verification process to guide the refinement of the abstraction granularity, which could further improve the robustness of the trained network.
\fi 
