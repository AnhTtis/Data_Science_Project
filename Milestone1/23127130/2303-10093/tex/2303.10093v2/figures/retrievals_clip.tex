\begin{figure}[t]
    \centering
    \includegraphics[scale=0.23]{images/ret_ex.PNG}
    \caption{\textbf{Example top region retrievals with CLIP and negative sampling strategies} (best viewed zoomed in and with color). In the top case, random adjectives retrieve \textit{black cup}, while baseline retrieves \textit{red cup} and order retrieves \textit{silver cup}. In the bottom row, baseline and random adjectives correctly retrieve \textit{red car}, while the order strategy incorrectly retrieves a \textit{red bookbag}.    } 
     
    \label{retrieval_clipEx}
\end{figure}