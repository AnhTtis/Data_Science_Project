\begin{table}[t]
  \centering
  \includegraphics[width=\columnwidth]{figures/sources/pandas_alts_results.pdf}
  \caption{\textit{Slowdown} when running common \code{pandas} operations with
  \code{pandas} alternatives. OOM Failure is out-of-memory failure and API
  incompatibility means that an API we used is not supported. As the results
  show, the current alternatives are not suited to ad-hoc, EDA workloads, both
  in terms of efficiency and usability.}
  \label{tbl:pandas_alt}
\end{table}