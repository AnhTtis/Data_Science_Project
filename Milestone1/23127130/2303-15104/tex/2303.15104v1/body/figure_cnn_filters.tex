\begin{figure}[h!]
    \centering
    \includegraphics[width=\linewidth]{figures/conv5_filters.png}
    \caption{Visualization for our feature extraction network. For each filter of the last 3D CNN layer (128 filters in total), we average the voxel grids ($16^{3}$) of top-100 training examples with the highest responses and extract surfaces from them using the marching cubes algorithm}
    \label{fig:cnn_filters}
\end{figure}
