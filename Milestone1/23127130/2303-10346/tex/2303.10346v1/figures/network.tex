
\begin{figure}[t] \centering
	\begin{overpic}[width=1.0\linewidth,tics=10]{figures/images/network}%,grid
   \end{overpic}
   \caption{Given an input point cloud $\mathcal{P}$, the aggregation layers generate the features $\{\mathcal{F}^\alpha$\}, $\alpha\in[1,5]$ at multiple network blocks. The propagation layers then propagate features from the input points to the sampled point $x$.
   }
   \label{fig:networks}
\end{figure} 