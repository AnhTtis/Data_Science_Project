% random vars
\def \latVar{\Theta}
\def \latVars{\boldsymbol{\Theta}}
\def \latVal{\theta}
\def \latVals{\boldsymbol{\theta}}
\def \measVal{z}
\def \measVals{\mathbf{z}}
\def \measVar{Z}
\def \measVars{\mathbf{Z}}

\def \odomVal{o}
\def \odomVals{\mathbf{o}}

\def \daVal{d}
\def \daVals{\mathbf{d}}

\def \idx{\mathcal{V}}

\def \obsVar{O}
\def \obsVal{o}
\def \lmkVar{L}
\def \lmkVal{l}
\def \rbtVar{X}
\def \rbtVal{x}
\def \lmkVars{\mathbf{\lmkVar}}
\def \rbtVars{\mathbf{\rbtVar}}
\def \rbtVals{\mathbf{\rbtVal}}
\def \lmkVals{\mathbf{\lmkVal}}

%The special Euclidean group
\DeclareMathOperator{\SE}{SE}
% Bayes tree
\def \clique{C}
\def \separator{S_\clique}
\def \frontal{F_\clique}
\newcommand{\tree}[1]{\mathcal{T}_{#1}}
\newcommand{\children}[1]{\mathcal{C}_{#1}}
\newcommand{\parents}[1]{\Pi_{#1}}
\def \pClique{\parents{\clique}}
\def \cClique{\children{\clique}}
\def \bayesTree{\tree{\clique}}

% graphical models
\def \allFactors{\mathbf{f}}
\def \potential{\psi}
\def \allE{\mathcal{E}}
\def \locg{\mathcal{G}_{\clique}}
\def \hybg{\mathcal{H}}

\DeclareMathOperator*{\argmax}{argmax}
\DeclareMathOperator*{\argmin}{argmin}
% vector slices
\newcommand{\leftSlice}[2]{{#1}_{<#2}}
\newcommand{\rightSlice}[2]{{#1}_{>#2}}
\def \uct{\mathcal{N}} % indices of uncertain landmarks
\def \gaus{\mathcal{G}} % indices of uncertain landmarks

% samlping
\newcommand{\sampling}[1]{{#1}\sim{p({#1})}}

% symbol
\def \bigdot{\boldsymbol{\cdot}}