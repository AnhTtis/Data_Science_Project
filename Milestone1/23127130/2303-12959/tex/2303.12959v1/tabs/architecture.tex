\begin{table}[t]
    \caption{The architecture details. ``FC.'' denotes fully connected layer, ``conv.'' denotes convolutional layer, ``deconv'' denotes transposed convolution layer. $c$ is the dimension of color channel.}
    \label{tab:architecture}
    \centering
    \begin{tabular}{lll}
        \toprule
         {Encoder} & {Decoder} \\
        % \midrule
        % Input \( 64 \times 64 \times 3 \) RGB image & Input \( \in\(   \)\mathbb{R}^{6} \) (3D Shapes) \( \mathbb{R}^{10} \) (CelebA, Chairs) \\
        \midrule
         \(4 \times 4\) conv. 32 stride 2 & FC.\@256 \\
        \midrule
         \(4 \times 4\) conv. 32 stride 2 & FC.\@ \(4 \times 4 \times 64 \) \\
        \midrule
         \(4 \times 4\) conv. 64 stride 2 & \(4 \times 4\) deconv. 64 stride 2 \\
        \midrule
        \(4 \times 4\) conv. 64 stride 2 & \(4 \times 4\) deconv. 32 stride 2 \\
        \midrule
         FC.\@ 256 & \(4 \times 4\) deconv. 32 stride 2\\
        \midrule
         FC.\@ 10 & \(4 \times 4\) deconv. \( c \) stride 2 \\
        \bottomrule
    \end{tabular}
\end{table}