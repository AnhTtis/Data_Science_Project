
\begin{figure}
\centering
\includegraphics[width=\linewidth]{pics/traversal.pdf}    
\caption[]{Latent traversal of DeVAE with the best MIG score on dSprites. Each block shows the generated images of traversing the latent variable from $-2$ to 2. \fix{}{Each group shows the traversal images sampling from 3 different random noise. The title above each group denotes the index of latent variable.}}
\label{fig:traversal}
\end{figure}
