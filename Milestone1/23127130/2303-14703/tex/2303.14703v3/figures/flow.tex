   \begin{adjustbox}{width=\linewidth}  
\begin{tikzpicture}[font=\normalsize,thick]
% Start block
\node[draw,
    %minimum width=2.5cm,
    minimum height=1cm] (kather) {TCGA-CRC-DX - n=360};

\node[draw,
    %minimum width=2.5cm,
    right of = kather,
    minimum height=1cm] (baseline) {Baseline model};

\node[draw,
    %minimum width=2.5cm,
    right of = baseline,
    minimum height=1cm] (molecular) {Molecular feature analysis};
\begin{comment}
\node[draw,
    minimum width=2.5cm,
    right of = molecular,
    minimum height=1cm] (BP-classes) {BP classes};

\node[draw,
    minimum width=2.5cm,
     below right of = BP-classes,
    minimum height=1cm] (SNP) {BP-CNN\textsubscript{SNP}};

\node[draw,
    minimum width=2.5cm,
    below left of = BP-classes,
    minimum height=1cm] (CIMP) {BP-CNN\textsubscript{CIMP}};

\node[draw,
    minimum width=2.5cm,
    below left of = SNP,
    minimum height=1cm] (Combined) {BP-CNN\textsubscript{Combined}};
\end{comment}
        
% Arrows
\draw[-latex] 
    (kather) edge (baseline);
\draw[->](baseline) -- (molecular);
%\draw[-latex](molecular) edge (BP-classes);
%\draw[-latex](BP-classes) edge (SNP);
%\draw[-latex](BP-classes) edge (CIMP);
%\draw[-latex](SNP) edge (Combined);
%\draw[-latex](CIMP) edge (Combined);

    


\end{tikzpicture}

\end{adjustbox}