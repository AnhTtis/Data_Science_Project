%\begin{proof}
    By induction on $s$: \begin{itemize}
        \item Case $s = \estate$. Then we can build $\Phi_s$ as follows:
        \[ \begin{prooftree}
            \infer0[(\ruleAx)]{\seqi{}{\estate}{\eset}{(0,0,0)}}
        \end{prooftree} \]
        And we can conclude with $\stype = \eset$ tight.
        \item Case $s = \upd{l}{v}{q}$. By the \ih, there exists $\Phi_q \tr \seqi{\Del_q}{q}{\stype_q}{(0,0,0)}$ tight. Therefore, we can build $\Phi_s$ as follows:
        \[ \begin{prooftree}
            \infer0[(\ruleMany)]{\seqi{}{v}{\emul}{(0,0,0)}}
            \hypo{\Phi_q \tr \seqi{}{q}{\stype_q}{(0,0,0)}}
            \infer2[(\ruleUpd)]{\seqi{}{\upd{l}{v}{q}}{\conj{(l : \emul)}; \stype_q}{(0,0,0)}}
        \end{prooftree} \]
        And we can conclude with $\stype = \conj{(l : \emul)}; \stype_q$ tight.
    \end{itemize}
%\end{proof}