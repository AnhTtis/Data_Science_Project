\begin{equation}
  z(y_{i, r}) = \log\left(
    d\left(
      \hat{\Pd}(y_{i, r} \mid \lambda, X_{r}, \boldsymbol{y}_{r}^{(\Pd)}),
      \tc(y_{i, r} \mid X_{r})
    \right)
  \right)
  + \log\left(
      \tp(y_{i, r} \mid X_{r})
    \right)
  - \log\left(
    \q(y_{i, r} \mid X_{r})
  \right),
  \label{eqn:log-discrep-func-def}
\end{equation}