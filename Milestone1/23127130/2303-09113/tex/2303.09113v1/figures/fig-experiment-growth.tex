\begin{figure}[tb]%
    \centering%
    \begin{tikzpicture}[]
        \footnotesize
        \begin{axis}[
                mysimpleplot, 
                xlabel={Blocks per sec. ($\bwtime$ alt. $\Delta^{-1}$)},
                ylabel={Honest chain growth rate\\ ($\blkratetimeGrowth/\blkratetimeHon$)},
                legend columns=2,
                xmin=0, xmax=2,
                ymin=0, ymax=0.8,
                height=0.5\linewidth,
                width=\linewidth,
                yticklabel style={
                        /pgf/number format/fixed,
                        /pgf/number format/precision=2
                    },
                scaled y ticks=false,
                grid = major,
                major grid style={solid,draw=gray!10},
            ]

            %
            %
            %
            %

            %
            %
            %
            %

            %
            %
            %

	

            \addplot [myparula12, mark size=1.5pt,only marks] table [x=inverse_delay,y=chain_growth] {figures/fig-experiment-growth-delay-data.txt};
            \addlegendentry{Bounded delay};
            \label{plt:experiment-growth-delay};

            \addplot [myparula73, mark size=1.5pt,only marks ] table [x=bandwidth,y=chain_growth] {figures/fig-experiment-growth-bandwidth-data.txt};
            \addlegendentry{Bandwidth limit};
            \label{plt:experiment-growth-bandwidth};


        \end{axis}
    \end{tikzpicture}%
    \vspace{-0.5em}%
    \caption{%
        The rate of chain growth (as a fraction of block production rate) for 100 identical nodes in the bounded-delay model (\ref{plt:experiment-growth-delay}) and in the bounded-bandwidth model (\ref{plt:experiment-growth-bandwidth}). No attacker is present. In the bounded-delay model the x-axis is $\Delta^{-1}$ (the inverse of the delay bound for delivering messages) which is interpreted as the effective rate at which a single block is delivered.
    }%
    \label{fig:experiment-growth}%
\end{figure}%