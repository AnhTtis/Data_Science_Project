\begin{scriptsize}
\begin{table}[h!]
    
\begin{scriptsize}
\begin{center}

    %\vspace{-3mm}
  \center
  \caption{Dataflow Configurations} 
  \label{table:dataflow_config}
    \vspace{-2mm}
\begin{tabular*}{|p{0.18\linewidth}|p{0.72\linewidth}|}
    \hline
    \textbf{Config} & \textbf{Description}  \\
    \hline
    %Seq & $V\times F$ & $t_{AGG}+t_{CMB}$\\
    %\hline
%    SEQ-Inner & Op-by-op Dataflow with Inner Product\\ \hline
    Flexagon~\cite{flexagon}-like*   
 & Flexagon's flexible architecture can optimally map a single (Op-by-op) operation with any shape and sparsity. It achieves  the best possible single operation AI. All ops begin and end in DRAM. Its the oracle operation-by-operation dataflow.\\
    \hline
    Adjacent Pipelining & It considers inter-operation pipelining between two consecutive operations when it is possible to apply (no delayed dependency). \reviewmehpca{We also consider tiling strategy proposed in~\autoref{sec:tiling}.}
      \\\hline
    GOGETA-\textsc{Qpad} & \DataflowNamenospace-\textsc{Qpad} does not consider pipelining, however, it writes the portion of the tensor that fits in the scratchpad in FIFO order proposed in~\autoref{sec:tornado} but does not consider distance/frequency based replacement. This ensures that parts of intermediate tensors that could fit, are still reused inside the SRAM.\\\hline
  %  GOGETA-df & GOGETA dataflow with inter-op pattern~\autoref{alg:inter-op} \\\hline
  %  & and loop orders~\autoref{alg:looporder}.\\\hline
    \DataflowNamenospace-\textsc{All} & \DataflowNamenospace-\textsc{all} uses all the contributions proposed. It uses the inter-op patterns proposed in~\autoref{sec:dataflows} and finds the individual loop orders which enable pipelining and avoid swizzle\_penalty proposed in~\autoref{sec:loop} and also uses the scratchpad management in~\autoref{sec:tornado}. It also uses scalable tiling strategy that reduces inter-cluster communication overhead proposed in~\autoref{sec:tiling}.ith infinite SRAM. (AI\textsubscript{best})\\\hline
    Ideal & Perfect reuse with infinte SRAM
    \\\hline
%    GOGETA+org & GOGETA-map with optimal tensor allocation ~\autoref{sec:tornado}\\\hline
    
    %Total L2 capacity & 32MB\\ \hline
    %Total L3 capacity & 256MB\\ \hline
    
  \end{tabular*}\end{center}

  *{While Flexagon proposes an accelerator for SpMSpM, we model Flexagon’s flexible NoC and microarchitecture which can run any single operation dataflow and model SpMM and \GEMM on it.}

\vspace{-3mm}





 % \vspace{1mm}
%\vspace{-4mm}
\end{scriptsize}

\end{table}

\end{scriptsize}