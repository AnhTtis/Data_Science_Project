
\begin{figure}[t]
    \centering
    \footnotesize
    \includegraphics[width=.5\textwidth]{images/intro_semantic_file.pdf}
    \caption{The illustration of the core idea in \algname. Besides the two matched pairs $\{I_1, T_1\}$ and $\{I_2, T_2\}$, we don't need extra annotation such as the middle pair. We purposely make image and text spaces with different lengths to highlight that the semantic changes in two spaces are not necessarily equal (Section~\ref{sec:method}). 
    }
    % \vspace{-3mm}
    \label{fig:intro_semantic}
\end{figure}